# A5000 x2 정밀도 실험: fp16 / fp8 / int8 / int4

날짜: 2026-08-03 · 하드웨어: NVIDIA RTX A5000 24GB x2 (Ampere SM 8.6, NVLink 없음 — 호스트 브리지 PCIe) · 스택: vllm-0.19.0

## 요약

- **저동시성에서는 int4가 압도적, 고동시성에서는 int8과 동급으로 수렴**합니다. Llama-8B 단일 스트림(c=1)에서 int4는 fp16의 **2.74배**(42→114 tok/s)지만, c=64에서는 1.39배로 떨어지고 int8(1.41배)에 추월당합니다. 디코드가 메모리 병목에서 연산 병목으로 넘어가며 dequantization 비용이 드러나는 지점입니다.
- **전력 효율은 양자화가 일관되게 이깁니다.** Llama-8B 피크에서 fp16 7.28 tok/J → int8 **10.60 tok/J(1.46배)**. 전력(약 190~206W)은 정밀도와 거의 무관했고, 이득은 전부 처리량에서 나옵니다.
- **NVLink가 없는 이 머신에서는 '양자화로 TP를 없애는' 편이 유리합니다.** Qwen-14B int4는 GPU 1장(TP1)에서 1100 tok/s로, GPU 2장 TP2(1296 tok/s)의 **85%**를 절반의 하드웨어로 냅니다 — GPU당 **1.70배**, 에너지 효율 **1.62배**. 같은 2장에 TP1 레플리카를 2개 띄우면 총 처리량이 TP2 대비 약 1.7배가 됩니다.
- **품질은 정밀도를 낮출수록 단조 감소하지만, 표본 200문제로는 유의하다고 말할 수 없습니다.** GSM8K: Llama-8B fp16 73.5% / fp8 72.0% / int8 70.5% / int4 70.0%, Qwen-14B(TP2) fp16 86.0% / fp8 80.5% / int8 85.0% / int4 80.5%. 최대 격차는 Qwen fp16→int4의 5.5pp인데 차이의 표준오차가 3.7pp라 약 1.5σ 수준입니다(4절 참고).
- **양자화는 KV 용량도 같이 사줍니다.** Llama-8B에서 할당된 KV가 47,920→120,624 토큰(2.5배)으로 늘어, 같은 카드에서 동시 요청 수나 컨텍스트 길이를 더 감당할 수 있습니다.

## 실험 설계

- **모델**: Llama-3.1-8B-Instruct(모든 정밀도가 1장에 적재 → TP 교란 없는 순수 정밀도 비교), Qwen2.5-14B-Instruct(fp16이 27.5 GiB라 **TP2 필수** → 양자화가 최소 병렬도를 바꾸는 사례)
- **부하**: vLLM `bench serve`, random 데이터셋 입력 256 / 출력 256 토큰, `--ignore-eos`, 동시성 스윕. 각 동시성마다 nvidia-smi로 전력을 병행 샘플링
- **품질**: GSM8K 테스트 200문제, 5-shot 고정 프롬프트, greedy, 마지막 숫자 매칭(`scripts/eval_gsm8k.py`). 리더보드 재현이 아니라 **동일 프롬프트에서 정밀도 간 상대 비교**가 목적
- **공통 설정**: `--dtype auto`(체크포인트의 양자화 설정 존중), max-model-len 4096, max-num-seqs 128, max-num-batched-tokens 2048, block-size 16, gpu-memory-utilization 0.90

### A5000(Ampere)의 정밀도 지원 특성

vLLM에 직접 질의한 결과 `supports_fp8() = False`(compute capability 8.6)입니다. 즉 **FP8 연산기가 없어** FP8 체크포인트는 Marlin weight-only 경로로 로드되어 메모리만 절약하고 계산은 FP16으로 수행됩니다. 반면 **INT8 W8A8은 INT8 Tensor Core를 실제로 사용**하고, INT4(AWQ)는 weight-only 양자화라 가중치 판독량을 1/4로 줄이는 방식으로 이득을 냅니다.

## 1. 메모리와 KV 용량

| 구성 | 정밀도 | 가중치(추정) | KV 캐시 토큰 | idle W | 로드된 GPU |
|---|---|---|---|---|---|
| Llama-3.1-8B-Instruct (TP1) | **fp16** | 15.0 GiB | 47,920 | 59.8 | 1x A5000 |
| Llama-3.1-8B-Instruct (TP1) | **fp8** | 7.5 GiB | 95,072 | 69.6 | 1x A5000 |
| Llama-3.1-8B-Instruct (TP1) | **int8** | 7.5 GiB | 95,056 | 73.6 | 1x A5000 |
| Llama-3.1-8B-Instruct (TP1) | **int4** | 4.2 GiB | 120,624 | 73.5 | 1x A5000 |
| Qwen2.5-14B-Instruct (TP2) | **fp16** | 27.5 GiB | 70,240 | 144.1 | 2x A5000 |
| Qwen2.5-14B-Instruct (TP2) | **fp8** | 13.8 GiB | 138,592 | 152.1 | 2x A5000 |
| Qwen2.5-14B-Instruct (TP2) | **int8** | 13.8 GiB | 138,352 | 152.0 | 2x A5000 |
| Qwen2.5-14B-Instruct (TP2) | **int4** | 7.8 GiB | 171,280 | 152.3 | 2x A5000 |
| Qwen2.5-14B-Instruct (TP1) | **int8** | 13.8 GiB | 23,040 | 74.1 | 1x A5000 |
| Qwen2.5-14B-Instruct (TP1) | **int4** | 7.8 GiB | 55,872 | 76.2 | 1x A5000 |

## 2. 처리량과 지연

#### Llama-3.1-8B-Instruct (TP1)

| 동시성 | fp16 tok/s | fp8 tok/s | int8 tok/s | int4 tok/s | fp16 TPOT(ms) | fp8 TPOT(ms) | int8 TPOT(ms) | int4 TPOT(ms) |
|---|---|---|---|---|---|---|---|---|
| 1 | 42 | 73 | 67 | 114 | 23.9 | 13.5 | 14.7 | 8.5 |
| 2 | 81 | 142 | 131 | 220 | 24.4 | 13.9 | 15.1 | 8.9 |
| 4 | 161 | 278 | 257 | 425 | 24.7 | 14.3 | 15.5 | 9.3 |
| 8 | 301 | 434 | 479 | 714 | 25.7 | 15.5 | 16.2 | 10.4 |
| 16 | 561 | 805 | 873 | 1230 | 27.2 | 16.4 | 17.5 | 11.7 |
| 32 | 1015 | 1450 | 1472 | 1720 | 29.5 | 19.9 | 20.3 | 16.9 |
| 64 | 1458 | 1967 | 2059 | 2021 | 38.9 | 29.1 | 28.7 | 28.2 |

#### Qwen2.5-14B-Instruct (TP2)

| 동시성 | fp16 tok/s | fp8 tok/s | int8 tok/s | int4 tok/s | fp16 TPOT(ms) | fp8 TPOT(ms) | int8 TPOT(ms) | int4 TPOT(ms) |
|---|---|---|---|---|---|---|---|---|
| 1 | 41 | 68 | 63 | 102 | 24.2 | 14.3 | 15.6 | 9.5 |
| 4 | 155 | 257 | 237 | 375 | 25.6 | 15.4 | 16.7 | 10.5 |
| 16 | 392 | 704 | 690 | 888 | 37.4 | 19.8 | 21.0 | 15.1 |
| 64 | 1088 | 1253 | 1305 | 1296 | 51.8 | 43.6 | 42.7 | 41.7 |

#### Qwen2.5-14B-Instruct (TP1)

| 동시성 | int8 tok/s | int4 tok/s | int8 TPOT(ms) | int4 TPOT(ms) |
|---|---|---|---|---|
| 1 | 37 | 65 | 26.6 | 15.1 |
| 4 | 144 | 246 | 27.6 | 16.1 |
| 16 | 502 | 710 | 29.9 | 19.2 |
| 64 | 884 | 1100 | 58.6 | 50.1 |

## 3. 전력 효율 (이 프로젝트의 핵심 지표)

`tok/J` = 피크 처리량 / 그 시점 평균 전력. 1M토큰 에너지는 해당 처리량·전력으로 100만 토큰을 생성할 때의 에너지입니다.

| 구성 | 정밀도 | 피크 처리량 | @동시성 | 그때 전력 | **tok/J** | 1M토큰 에너지 | 같은 모델 fp16 대비 |
|---|---|---|---|---|---|---|---|
| Llama-3.1-8B-Instruct (TP1) | **fp16** | 1458 tok/s | c=64 | 200 W | **7.28** | 38.2 Wh | 1.00x |
| Llama-3.1-8B-Instruct (TP1) | **fp8** | 1967 tok/s | c=64 | 196 W | **10.03** | 27.7 Wh | 1.38x |
| Llama-3.1-8B-Instruct (TP1) | **int8** | 2059 tok/s | c=64 | 194 W | **10.60** | 26.2 Wh | 1.46x |
| Llama-3.1-8B-Instruct (TP1) | **int4** | 2021 tok/s | c=64 | 194 W | **10.42** | 26.7 Wh | 1.43x |
| Qwen2.5-14B-Instruct (TP2) | **fp16** | 1088 tok/s | c=64 | 410 W | **2.65** | 104.8 Wh | 1.00x |
| Qwen2.5-14B-Instruct (TP2) | **fp8** | 1253 tok/s | c=64 | 401 W | **3.12** | 88.9 Wh | 1.18x |
| Qwen2.5-14B-Instruct (TP2) | **int8** | 1305 tok/s | c=64 | 399 W | **3.27** | 85.0 Wh | 1.23x |
| Qwen2.5-14B-Instruct (TP2) | **int4** | 1296 tok/s | c=64 | 398 W | **3.25** | 85.4 Wh | 1.23x |
| Qwen2.5-14B-Instruct (TP1) | **int8** | 884 tok/s | c=64 | 212 W | **4.17** | 66.7 Wh | 1.57x |
| Qwen2.5-14B-Instruct (TP1) | **int4** | 1100 tok/s | c=64 | 209 W | **5.26** | 52.8 Wh | 1.98x |

## 4. 품질 (GSM8K)

| 구성 | 정밀도 | GSM8K (200문제, 5-shot, greedy) | fp16 대비 | 요청 오류 |
|---|---|---|---|---|
| Llama-3.1-8B-Instruct (TP1) | **fp16** | 73.5% (147/200) | +0.0pp | 0 |
| Llama-3.1-8B-Instruct (TP1) | **fp8** | 72.0% (144/200) | -1.5pp | 0 |
| Llama-3.1-8B-Instruct (TP1) | **int8** | 70.5% (141/200) | -3.0pp | 0 |
| Llama-3.1-8B-Instruct (TP1) | **int4** | 70.0% (140/200) | -3.5pp | 0 |
| Qwen2.5-14B-Instruct (TP2) | **fp16** | 86.0% (172/200) | +0.0pp | 0 |
| Qwen2.5-14B-Instruct (TP2) | **fp8** | 80.5% (161/200) | -5.5pp | 0 |
| Qwen2.5-14B-Instruct (TP2) | **int8** | 85.0% (170/200) | -1.0pp | 0 |
| Qwen2.5-14B-Instruct (TP2) | **int4** | 80.5% (161/200) | -5.5pp | 0 |
| Qwen2.5-14B-Instruct (TP1) | **int8** | 85.0% (170/200) | — | 0 |
| Qwen2.5-14B-Instruct (TP1) | **int4** | 79.5% (159/200) | — | 0 |

## 5. 분석

### 5.1 정밀도의 이득은 동시성에 따라 뒤집힙니다

Llama-8B TP1, fp16 대비 처리량 배수:

| 동시성 | fp8 | int8 | int4 |
|---|---|---|---|
| 1 | 1.75x | 1.62x | 2.74x |
| 2 | 1.75x | 1.61x | 2.71x |
| 4 | 1.73x | 1.59x | 2.64x |
| 8 | 1.44x | 1.59x | 2.37x |
| 16 | 1.43x | 1.56x | 2.19x |
| 32 | 1.43x | 1.45x | 1.70x |
| 64 | 1.35x | 1.41x | 1.39x |

int4의 우위는 c=1의 2.7배에서 c=64의 1.4배로 단조 감소하고, int8은 1.6→1.4배로 거의 평탄합니다. 두 곡선이 c=32~64 사이에서 교차합니다. 해석: 배치가 작을 때 디코드는 **가중치 판독 대역폭**이 병목이라 4비트가 그대로 이득이 되지만, 배치가 커지면 같은 가중치 판독을 여러 요청이 분담하므로 병목이 연산으로 옮겨가고 int4의 실시간 역양자화 비용이 남습니다. W8A8(int8)은 활성값까지 8비트라 고배치에서 INT8 Tensor Core를 실제로 쓸 수 있어 그 구간을 가져갑니다.

**운영 함의**: 저지연·저동시성(코딩 어시스턴트, 대화형)에는 int4, 고스루풋 배치 서빙에는 int8이 유리합니다. 하나만 고른다면 int8이 두 구간에서 모두 상위권이며 품질 손실도 가장 작았습니다.

### 5.2 Ampere에서 FP8은 '메모리 절약'까지만입니다

측정값은 이 예상과 일치합니다. c=1에서는 fp8 73 vs int8 67 tok/s로 사실상 동급(둘 다 8비트 가중치를 읽는 메모리 병목 구간)이지만, c=64에서는 int8 2059 > fp8 1967 tok/s로 int8이 앞섭니다 — INT8 Tensor Core를 실제로 쓸 수 있는 쪽이 고배치에서 이깁니다. FP8 연산기가 있는 Ada/Hopper에서는 이 순서가 바뀔 것으로 예상되며, 그 검증은 해당 하드웨어가 필요합니다.

### 5.3 양자화 vs 텐서 병렬 (PCIe 환경)

| 동시성 | int8 TP1(1GPU) | int8 TP2(2GPU) | int8 GPU당 이득 | int4 TP1(1GPU) | int4 TP2(2GPU) | int4 GPU당 이득 |
|---|---|---|---|---|---|---|
| 1 | 37 | 63 | **1.19x** | 65 | 102 | **1.27x** |
| 4 | 144 | 237 | **1.22x** | 246 | 375 | **1.31x** |
| 16 | 502 | 690 | **1.45x** | 710 | 888 | **1.60x** |
| 64 | 884 | 1305 | **1.36x** | 1100 | 1296 | **1.70x** |

GPU당 이득이 **동시성과 함께 커집니다**(int4: 1.27→1.70배). all-reduce 전송량이 배치에 비례해 늘기 때문에, 호스트 브리지 PCIe(실측 분류 ~12GB/s)에서는 배치가 클수록 TP2의 손실이 커집니다. 같은 모델을 GPU 1장에 담을 수 있다면 **TP를 쓰지 않고 레플리카를 늘리는 편이** 처리량·전력 모두 유리합니다.

**단, TP2가 사는 지점은 KV 용량입니다.** int4 TP2는 KV 171,280 토큰인데 TP1은 55,872 토큰으로 3배 차이입니다. 긴 컨텍스트나 레플리카당 높은 동시성이 필요하면 TP2가 여전히 필요하고, int8 TP1은 KV가 특히 빠듯해(가중치 13.8GiB / 예산 21.6GiB) 실사용 여유가 작습니다.

### 5.4 품질: 어디까지 신뢰할 수 있나

GSM8K 200문제에서 비율의 표준오차는 약 3pp, **두 설정 차이의 표준오차는 약 4pp**입니다. 따라서:

- Llama-8B의 최대 격차(3.5pp)는 **노이즈와 구분되지 않습니다**. 이 표본으로는 '8B에서 int4가 품질을 떨어뜨렸다'고 말할 수 없습니다.
- Qwen-14B는 int8이 fp16과 거의 같은데(86.0% vs 85.0%) fp8과 int4가 함께 80.5%로 내려갔습니다. 약 1.5σ라 단정은 못 하지만 **방향성은 int8 우세**입니다. fp8이 int4만큼 떨어진 것은 포맷 자체보다 체크포인트 캘리브레이션 차이일 가능성이 큽니다(서로 다른 조직이 만든 체크포인트).
- 같은 int4 가중치를 TP1/TP2로 돌린 결과가 80.5% vs 79.5%로 1.0pp 달랐습니다. 이 값이 곧 **측정 노이즈의 하한 눈금**입니다(가중치가 동일하므로).

확정적 품질 결론이 필요하면 문제 수를 1,000개 이상으로 올리고 (설정당 약 10분 추가) MMLU 등 다른 과제를 병행해야 합니다.


## 6. 한계

- GSM8K 200문제·5-shot 단일 프롬프트이므로 절대 점수는 공개 리더보드와 다릅니다. 정밀도 간 **상대 비교**로만 해석하십시오(±3pp 내 차이는 표본 오차와 구분되지 않습니다).
- 입력/출력 길이를 256/256으로 고정했습니다. 긴 컨텍스트에서는 KV 용량 차이가 더 크게 작용합니다.
- 전력은 nvidia-smi 샘플링(GPU만)이며 호스트 전력은 포함하지 않습니다.
- 동일 모델의 양자화 체크포인트는 서로 다른 조직이 서로 다른 캘리브레이션 데이터로 만든 것이라, 정밀도 자체의 효과와 체크포인트 품질 차이가 섞여 있습니다.

## 재현

```bash
python scripts/run_precision_study.py              # 전체 매트릭스
python scripts/run_precision_study.py llama8b-int4 # 개별 설정
python scripts/make_precision_report.py --out docs/PRECISION_STUDY_A5000.md
```
