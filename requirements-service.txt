# Service-layer dependencies (PLAN_power_service.md M0).
# The planner deps (requirements-planner.txt) are also required.
fastapi
uvicorn
pydantic>=2
pyyaml
docker      # deployment layer (execution plan D2)
networkx    # topology graph (also in requirements-planner.txt)
